to install:

    virtualenv-2.7 venv
    . venv/bin/activate
    pip install -r requirements.txt

to confirm everything is installed you can do:
    ipython
    import cli_insert
    (control D return to quit)

